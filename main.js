var typed=new Typed(".text",{
    strings: [" Frontend Developer"," Web Developer"],
    typeSpeed: 50,      // Lower = faster typing
    backSpeed: 30,      // Lower = faster deleting
    backDelay: 50,
    loop:true
});